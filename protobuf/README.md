## Protobuf 的使用

### 简要概述
protobuf 是一个使用特定语法定义通信规则后，生成对应的实现代码，对于需要通信的数据，在客户端进行数据序列化，在服务端将数据进行反序列化得到对应的数据结构。序列化的方式有很多比如json,xml，但是protobuf具有性能高，所占的空间极小的显著优势，被广泛使用

### 遇到的问题

- 对于某些特定的版本编译会异常，添加-O2 -DNDEBUG后解决